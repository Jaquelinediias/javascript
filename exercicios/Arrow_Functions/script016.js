/*function somar(y, x) {
    return y + x;
}
console.log(somar(10, 89));
*/

// Segunda forma de fazer uma função.
/*
let somar  = function(x, y){
   return x + y;
}
*/

// Terceira forma de fazer uma função.
/*
let somar =(x, y) => {
    return x + y;
}
*/

/*
let somar = (x, y) => x + y;
*/

/*let letrasNome = (nome) => {
    return nome.length;
}
*/

let letrasNome = (nome) => nome.length;

console.log(letrasNome('JaquelineDias'));

// A unica diferença entre uma função normal e uma anônima(arrow), a gente não tem o objeto This.


